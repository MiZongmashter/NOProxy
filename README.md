# NOProxy

这是一个用于存放直连规则的仓库。

- 规则文件：NOProxy
- 作用：把指定域名/网站加入直连列表，不走代理
- 适用场景：Clash、Surge、Sing-Box 等支持规则文件的工具

使用方式：
1. 在 NOProxy 文件中追加规则，例如：
   DOMAIN-SUFFIX,example.com
2. 保存后提交并同步到仓库即可

如需添加新规则，可直接在该文件中继续写入。
